{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the page is allowed to ask the shell for. Nothing yet: the application talks to a database and draws, and needs no more of the machine than a browser tab gets.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
